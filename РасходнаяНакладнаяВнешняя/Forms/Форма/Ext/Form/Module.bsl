&НаКлиенте
Процедура Сформировать(Команда,Табдок)

	СформироватьНаСервере(Табдок);

	ТабДок.Показать();

КонецПроцедуры

&НаСервере
Процедура СформироватьНаСервере(Табдок)
	
	ОбъектОбработка = РеквизитФормыВЗначение("Объект");

	МассивОбъектов = Новый Массив;

	МассивОбъектов.Добавить(Объект.СсылкаНаОбъект);

	ТабДок = ОбъектОбработка.СформироватьПечатнуюФормуРасходнаяНакладная(МассивОбъектов, Неопределено);

КонецПроцедуры

